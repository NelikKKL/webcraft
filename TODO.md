# TODO — план следующих этапов

## СЛЕДУЮЩАЯ СЕССИЯ НАЧИНАЕТ ЗДЕСЬ

Архитектура готова (GL-мост + AWT/ImageIO/ресурсы), но ФИЗИЧЕСКОЕ дерево
`net/minecraft/client/*` (519 файлов) ещё НЕ скопировано в
`web-port/src/main/java/` — это чисто механический, но большой шаг,
сознательно отложенный в конце этой сессии (см. обоснование ниже, раздел
"Почему копирование дерева отложено").

Чеклист для следующей сессии:
1. Скопировать `net/minecraft/client/**` и `net/minecraft/isom/**` (кроме
   исключённых, см. PATCHES.md) в `web-port/src/main/java/net/minecraft/`.
2. Применить все точечные патчи из `PATCHES.md` (8 однострочных замен —
   можно сделать через `sed`/Python-скрипт, все замены механические).
3. Удалить/не копировать файлы из списка "Полностью исключённые" в
   PATCHES.md — они либо не нужны (изометрический previewer, AWT-окна),
   либо сознательно отложены (звук, ZIP текстур-паки).
4. НЕ копировать оригинальные `MinecraftApplet.java` и `qg.java` — они уже
   написаны заново в `web-port/src/main/java/net/minecraft/client/` (наши
   версии не должны быть перезаписаны копированием).
5. Переписать `oi.java` при копировании — заменить `this.c.getWidth()`/
   `getHeight()` на `org.lwjgl.opengl.Display.getWidth()`/`getHeight()`,
   убрать поле `Component c` и адаптировать конструктор (см. PATCHES.md).
6. Написать `WebMinecraft extends Minecraft` (наш аналог `i.java`/
   `StandaloneClient.java`) — реализует абстрактный метод `a(hr)` через
   `console.error`/DOM вместо AWT CrashDialog.
7. Переписать `WebEntryPoint.main()`: сначала
   `ResourcePreloader.preloadAll(ResourceManifest.IMAGE_PATHS, () -> { ... })`,
   и ТОЛЬКО в колбэке — создание `WebMinecraft` и запуск игрового цикла
   (см. текущий `WebEntryPoint.java` — там пока только smoke-test сцена,
   её нужно будет заменить/дополнить).
8. Прогнать через CI, ожидать МНОГО ошибок компиляции на первый раз —
   некопированные/неучтённые зависимости неизбежны при таком объёме кода.
   Идти по ошибкам итеративно, каждую — либо патчить точечно (добавляя в
   PATCHES.md), либо это сигнал, что нужен ещё один заглушенный класс
   (по аналогии с `qg`/`MinecraftApplet`).
9. Отдельно проверить: использует ли сохранение МИРОВ `java.io.File`
   (см. "Этап 3.5" ниже) — если да, это ещё один блокер компиляции/
   функциональности, требующий моста на localStorage/IndexedDB.

## Почему копирование дерева отложено в этой сессии

519 файлов — это объём, который нельзя osмысленно скопировать и
пропатчить "вслепую" одним скриптом без риска потерять по пути ещё
несколько находок уровня "VBO путь мёртв"/"disableClientState обязателен"/
"oi.java кинет NPE" — каждая из которых была найдена только благодаря
чтению конкретного файла, а не угадыванием. Учитывая это, приоритет этой
сессии был: (а) выстроить архитектуру мостов (GL, ввод, AWT, ресурсы),
которая реально выдержит интеграцию, и (б) найти и задокументировать
максимум подводных камней ЗАРАНЕЕ, а не бросаться копировать 519 файлов
без понимания, что как минимум 20-30 из них (AWT-окна, звук, ZIP-паки,
сетевая загрузка скинов, скриншоты) требуют осознанных решений, а не
копипасты.

## Немедленно после первого push (проверка сборки)

- [ ] Прогнать CI, посмотреть на реальные ошибки TeaVM-компиляции.
- [ ] Особое внимание: `Float32ArrayFactory`/`Uint8ArrayFactory` —
      использован путь через `new ArrayBuffer(n)` + `new Float32Array(buf)`/
      `new Uint8Array(buf)`, это должно быть стабильным API в любой версии
      TeaVM jso-apis, но не проверено сборкой.
- [ ] Проверить, что `teavm-maven-plugin` версия `0.10.2` существует в Maven
      Central на момент сборки (версии TeaVM выходят часто, могла устареть
      уже к моменту чтения этого файла) — при ошибке "artifact not found"
      просто поднять/опустить версию в `pom.xml` (`teavm.version`).
- [ ] Проверить, что сгенерированный `alpha126.js` реально самозапускается
      при загрузке страницы (некоторые версии TeaVM генерируют ES-модуль,
      требующий явного `import` + вызова `main()` из `index.html` — если
      экран пустой и в консоли браузера тишина, это первое, что проверять).

## Этап 2 — AWT/ImageIO мост (ГОТОВО в этой сессии)

Реализовано: `java.awt.image.BufferedImage` (getRGB/setRGB bulk-форма,
getGraphics, getRaster), `java.awt.Graphics` (drawImage/setColor/fillRect/
dispose), `java.awt.Color` (настоящая HSBtoRGB математика — используется
для цвета неба/биомов, НЕ мёртвый код), `java.awt.image.WritableRaster`/
`DataBuffer`/`DataBufferInt` (для lf.java, конвертер старых скинов).

НЕ реализовано (сознательно, задокументировано в PATCHES.md): полноценный
AWT windowing (Canvas/Frame/Component/Panel/TextArea/Toolkit) — вместо
этого исключены файлы, которые в них нуждаются (CrashDialog, i.java,
j.java, StandaloneClient.java, gn.java, ow.java, MinecraftApplet — все
заменены/исключены, см. PATCHES.md). Буфер обмена (`bp.java`,
DataFlavor/Transferable) — тоже отложено, не тронуто.

## Этап 3 — Загрузка ресурсов (ГОТОВО в этой сессии, для картинок)

Реализовано:
1. Полный манифест из 44 картиночных путей (`ResourceManifest.java`),
   извлечён построчным grep всех строковых литералов `"/path/to/x.png"`
   по всему decomp-дереву — ВКЛЮЧАЯ пути, переданные не напрямую в
   `getResource(...)`, а через промежуточную переменную (нашли так
   `/font/default.png`, изначальный более узкий grep его пропустил).
2. Все 44 файла скопированы в `web/assets/` с сохранением структуры путей.
3. `ResourcePreloader.java` — асинхронный fetch + `createImageBitmap` +
   `canvas.getImageData()` (декодирование PNG браузером, НЕ Java-кодом —
   писать свой PNG-декодер не нужно) для ВСЕХ путей разом, ДО старта
   игрового кода.
4. `ResourceCache.java` — синхронный Map<путь, ARGB int[]>, заполняется
   один раз результатами прелоадки.
5. `javax.imageio.ImageIO` — свой `read(String)` (новая, изобретённая форма
   — не существует в реальном ImageIO, введена чтобы патчи call-сайтов
   были однострочными, см. PATCHES.md) + `read(InputStream)` для пути через
   `d.java` (единственная точка, где реально нужен объект-поток, а не
   просто путь) — оба читают из ResourceCache синхронно.
6. `ResourceIO.java` + `PATCHES.md` — 8 точечных патчей в decomp-файлах,
   заменяющих `Xxx.class.getResource(...)`/`getResourceAsStream(...)` на
   наш мост (см. PATCHES.md — почему не полагаемся на поведение
   `java.lang.Class.getResourceAsStream` в TeaVM: непроверяемо в этой
   среде разработки).

НЕ реализовано (см. PATCHES.md, "Отложенные"): `em.java` (ImageIO.write
скриншота — нужен Blob+download вместо File), `mq.java` (сетевая загрузка
скинов через HttpURLConnection — CORS-проблема), `/title/splashes.txt`
(текстовый ресурс — ResourcePreloader сейчас прелоадит только картинки).
`od.java` (ZIP-based текстур-паки) — исключён из сборки целиком.

## Этап 3.5 — НОВОЕ: персистентность (File I/O) — не начато, важно

При проверке `qg.java` (звук) обнаружилось, что `gq.java` (GameSettings)
тоже использует `File`/`FileReader`/`FileWriter`/`PrintWriter` для
сохранения настроек — и, по аналогии, сохранение МИРОВ в Alpha тоже почти
наверняка идёт через `java.io.File` (не проверено в этой сессии — нужно
явно перепроверить перед интеграцией мира/сохранений). Браузер не даёт
прямого доступа к файловой системе — нужен мост `java.io.File`-подобного
API на `localStorage` (для маленьких данных типа настроек) или
`IndexedDB` (для потенциально больших данных — сохранения миров). Это
отдельный, нетривиальный кусок работы, не начатый. Если TeaVM classlib
уже включает рабочий `java.io.File` с каким-то built-in виртуальным
хранилищем — стоит проверить это ПЕРВЫМ делом перед написанием своего
моста (может оказаться, что делать нечего или почти нечего).

## Этап 4 — Звук (заглушка есть, реализация не начата)

`qg.java` заменён no-op заглушкой с идентичными публичными сигнатурами
(см. PATCHES.md) — код, вызывающий звук, компилируется, но ничего не
играет. paulscode + JOrbis/JOgg (26+ файлов) полностью исключены из
сборки. Настоящая реализация — через Web Audio API, отдельная сессия.
Ключевой вопрос на будущее: WAV/OGG файлы самих звуков (не найдены в
использованном для этой сессии манифесте ресурсов — вероятно, грузятся
динамически по путям, которые ещё предстоит извлечь тем же методом
grep-по-литералам, что и для текстур, но по .ogg/.wav расширениям).

## Этап 5 — Потоки

Оригинал использует несколько `Thread` (см. `new gi(this, "Timer hack
thread")` в конструкторе `Minecraft`, плюс, вероятно, поток загрузки
чанков). TeaVM JS-таргет однопоточный (WASM GC таргет может поддерживать
потоки по-другому — не исследовано). Варианты:
- Web Workers для реально тяжёлых фоновых задач (генерация чанков).
- Для лёгких периодических задач (timer hack) — просто
  `setInterval`/переосмысление через `requestAnimationFrame`-цикл.

## Этап 6 — Интеграция реального Minecraft.java

После этапов 2–5: подключить настоящее дерево `net/minecraft/client/*`,
скорее всего начиная с рендера мира (World/Chunk/Tessellator — уже
частично проверено через `is.java`, наш GL-мост готов к его паттернам)
и постепенно добавляя игровую логику (мобы, инвентарь, GUI-экраны).

GUI-экраны (`javax.swing`-независимые, судя по всему используют
собственный immediate-mode рендер через GL11 — нужно перепроверить)
скорее всего заработают "бесплатно" вместе с GL-мостом, если не тянут
AWT напрямую.

## Известные упрощения текущей реализации (не блокеры, но стоит помнить)

- `GLU.gluPerspective` — единственный реализованный метод GLU; если
  реальный код использует `gluLookAt`/`gluProject`/`gluUnProject`
  (не встречено в grep, но перепроверить при интеграции полного дерева),
  нужно будет доreализовать.
- `Controllers` (геймпады) — полный no-op.
- `ARBVertexBufferObject` — методы кидают `UnsupportedOperationException`
  (обоснование в javadoc класса — путь мёртвый в этой сборке decomp, но
  ПЕРЕПРОВЕРИТЬ при интеграции: если окажется, что где-то `c` на самом
  деле не всегда false в более позднем/другом коде, придётся реализовать
  по-настоящему через WebGL2 buffer objects, благо GLState уже умеет
  создавать/биндить WebGLBuffer).
- `Display.setParent` — no-op (веб-canvas всегда один и статичен в DOM).
- `GL11.glLight`/`glLightModel` реализуют только 2 источника света без
  специального цвета (specular) — упрощённая модель, достаточная для
  того уровня fixed-function освещения, что использует Alpha 1.2.6
  (проверить при полной интеграции, не понадобится ли больше).
